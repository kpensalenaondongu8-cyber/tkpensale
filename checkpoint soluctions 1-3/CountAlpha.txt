package piscine

func CountAlpha(s string) int {
	count := 0

	for _, r := range s {
		// Check if r is a letter (A–Z or a–z)
		if (r >= 'A' && r <= 'Z') || (r >= 'a' && r <= 'z') {
			count++
		}
	}

	return count
}
