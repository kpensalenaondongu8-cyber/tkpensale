package main

import (
    "os"
)

func main() {
    if len(os.Args) < 2 {
        os.Stdout.WriteString("\n")
        return
    }
    result := ""
    for _, arg := range os.Args[1:] {
        result += arg
    }
    os.Stdout.WriteString(result + "\n")
}

